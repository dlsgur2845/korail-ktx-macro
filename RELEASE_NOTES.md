## 2.0.4 — 예매·예약대기 재시도 대기 단축

- 명확한 잔여석 없음·예약대기 한도 초과 응답 뒤 최소 1초 및 조회 후 대기 설정을 적용하던 지연을 제거했습니다.
- 안내창이 닫히고 로딩이 끝나면 동일 열차 선택과 버튼 활성화를 120ms 확인한 후 다시 신청합니다. 요청 처리 중에는 추가 제출하지 않습니다.

## 2.0.3 — 1명 예약으로 단순화

- 다음 1명 시작 버튼과 여러 명 예약용 실행 상태를 제거했습니다. 웹 조회 인원은 1명만 허용하며 접수 완료 후 정지합니다.
- 시작 버튼을 하나로 통합했습니다. 이전 접수 기록이 있으면 내역 확인 후 새 감시를 시작할 수 있습니다.
- 여러 열차 감시와 같은 열차의 명확한 잔여석 없음·예약대기 한도 초과 재시도는 유지합니다.

## 2.0.2 — 잔여석없음 재시도 및 실행 중 절전 방지

- 실제 Chrome에서 표시된 ‘잔여석없음’을 알 수 없는 안내로 취급하던 문제를 수정했습니다. 확인 후 같은 열차의 예매 버튼만 재시도합니다.
- 일반 감시 및 명절 정시 클릭 대기 중 화면·시스템 자동 절전을 방지합니다. power 권한이 추가됩니다.
- 중지·완료·탭 종료 시 해제하며, 여러 탭 중 실행 중인 작업이 있으면 유지합니다. 새로고침·서비스 워커 재시작에도 유지하고 응답 없는 탭은 만료 처리합니다.
- 모의 테스트 106개 통과. 실제 서버 재시도와 Windows/macOS 절전 방지 실동작은 미검증입니다.

## 2.0.1 — 선택됐는데 클릭 기록 누락으로 멈추는 문제 수정

- 실제 Chrome에서 019 예약대기 선택과 하단 신청 버튼 활성화를 확인했으나, 클릭 이벤트 누락만으로 중지한 문제를 수정했습니다.
- 좌석 선택 클릭의 이벤트가 누락되면 최대 0.5초간 동일 열차·구간·시각의 실제 선택 표시를 확인합니다. 선택이 확인되면 재클릭 없이 다음 단계로 진행합니다.
- 선택 표시도 없거나 예매 제출 클릭의 이벤트가 누락되면 계속 정지합니다. 불명확한 제출을 재전송하지 않습니다.
- 일반실·예약대기 선택 누락, 선택 미반영, 제출 이벤트 누락을 모의 테스트했습니다. 실제 서버 접수는 미검증입니다.

# 릴리스 노트

[설치·사용법으로 돌아가기](README.md)

최신 버전부터 정리한 변경 이력입니다. 각 항목은 해당 버전 출시 당시의 동작을 설명하며, 현재 사용법은 README를 참고하세요.

## 2.0.0 — 고정 감시 패널과 설정 팝업

- 명절 정시 클릭 시험 기능: 기존 설정 팝업의 명절 탭 또는 명절 페이지의 자동 표시 패널에서 설정을 열고, 마우스 테두리로 버튼을 선택합니다. 선택 중 실제 클릭·누름을 차단하고 한국 시각을 지정해 한 번 클릭합니다. 버전은 2.0.0을 유지합니다.
- 서버 HTTP Date와 왕복 지연을 측정하며, 지연만큼 조기 클릭하지 않습니다. 초 단위 시각 오차 여유를 적용하므로 정각보다 늦을 수 있습니다. 실제 예약 서버와의 시계 일치와 밀리초 정밀도를 보장하지 않습니다. 시각 측정 실패·탭 숨김·페이지 변경·버튼 변경·타이머 지연 시 취소하고 불명확한 클릭은 재시도하지 않습니다.
- 명절 기능을 기존 팝업 조작 방식으로 통합했습니다. 대상 선택 시 팝업을 잠시 닫고 선택 후 복귀합니다. 준비 완료 후 닫힌 상태에서도 대기가 계속되고, 실제 입력 직전 팝업이 자동으로 닫혀 페이지 클릭을 방해하지 않습니다. 중단은 취소 버튼으로 합니다.
- HTTPS 코레일 및 하위 도메인에 명절 설정 패널을 자동 표시합니다. 일반 예매 스크립트의 자동 주입 범위는 유지합니다. `scripting` 권한을 제거하고 확장 아이콘의 보조 진입점에 `activeTab`을 사용합니다. 실제 명절 서버는 미검증이며 모의 페이지에서 검증합니다.
- 명절 기능 검증: 시각 경계·지연·캐시/만료 응답·발신자·문서 변경·취소·단일 입력 테스트와 로그 검증을 추가했습니다. 모의 브라우저에서 선택 시 클릭/누름 0회, 지정 시각 후 클릭 1회, 취소 후 클릭 0회를 확인했습니다. 360×480 화면의 패널 경계도 확인했습니다. 전체 테스트 11개 파일이 통과했습니다.
- 로그 목록의 개별 파일 크기와 총용량을 B·KB·MB·GB 등으로 자동 표시합니다(1 KB = 1,000 B, 소수점 최대 2자리). 마우스를 올리면 정확한 바이트 수를 확인할 수 있습니다.
- 감시 패널을 대상 열차·진행 상태·하단 조작 영역으로 재구성했습니다. 같은 뷰포트에서 새로고침하거나 내용 길이가 바뀌어도 패널 높이와 시작·중지 버튼 위치를 유지합니다.
- 조회 설정·알림·진단 기록을 탭이 있는 팝업으로 옮겼습니다. 작은 화면에서는 내부 내용만 스크롤하고 닫기·완료 버튼은 고정합니다. 페이지 스크롤바를 제외한 너비에 맞춰 가장자리 잘림도 방지합니다.
- 중지 상태의 조회 설정을 탭 세션에 자동 저장합니다. 새로고침 후 복원하며, 실행 중에는 실제 감시 조건을 표시하고 조회 설정 변경을 막습니다.
- 팝업의 Escape 닫기, 원래 버튼으로 초점 복귀, 방향키·Home·End 탭 이동을 지원합니다. 패널 최소화 시 팝업도 닫힙니다.
- 조회·새로고침 간격과 1.8.4의 예매 버튼 대기 단축 동작은 유지합니다.
- README를 설치·화면 안내·사용법 중심으로 정리하고 새 UI 이미지와 동작 흐름 그림을 추가했습니다. 이전 업데이트 내역은 이 문서에 모았습니다.
- 검증: 자동 테스트 10개 파일 통과. 생산 코드 기반 모의 브라우저에서 1280×720 / 360×480 배치, 열차 수 변경·새로고침 시 버튼 위치, 설정 복원, 키보드 탭 이동·Escape 닫기를 확인했습니다. 실제 예매와 Windows·Mac 양쪽 실사용 검증은 수행하지 않았습니다.

## 1.8.4 — 예매 버튼 대기 단축

- 예매·예약대기 최종 신청 버튼의 안정화 대기를 800ms에서 120ms로 줄였습니다.
- 조회/새로고침 간격과 실패 후 재시도 간격은 유지합니다. 로딩 또는 예약대기 좌석 옵션 변경 시 버튼 안정화 시간을 다시 확인합니다.

## 1.8.3 — 예매 반복 확인 및 ntfy 상세 알림

- 후속 UI 수정: 패널 바깥의 스크롤바 예약 여백을 제거해 오른쪽 빈 띠와 헤더·하단 영역의 폭 어긋남을 수정했습니다.

- 일반 예매의 잔여석 없음 → 확인 → 동일 열차 예매 반복을 회귀 검증했습니다. 새로고침·열차 재선택 없이 응답과 화면 준비를 기다리며 성공 시 정지합니다.
- 휴대폰 알림을 좌석 발견·예약 완료 문구 확인·예약대기 접수·결과 확인 필요·감시 중지로 구분했습니다.
- 열차 번호·날짜·구간·출발시각·등급·재시도 횟수와 분류된 중지 사유를 ntfy.sh에 보냅니다. 예약번호·계정 정보·쿠키·오류 원문은 제외합니다.
- ntfy 기차 이모지와 일반 알림 제목의 장식 이모지를 제거했습니다. Chrome PC 알림의 필수 앱 아이콘은 유지합니다.
- 자동 테스트로 페이로드와 재시도 동작을 검증했습니다. 아이폰 실제 표시와 서버 경쟁 상황은 미검증입니다.

## 1.8.2 — 예약대기 한도 안내 후 동일 열차 재시도

- 예약대기 초기 신청의 정확한 ‘예약대기자한도수초과’ 응답은 확인 후 같은 하단 예약대기신청 버튼을 재시도합니다.
- 선택 열차를 유지하고 새로고침·열차 재선택 없이 진행합니다. 안내창 종료·화면 준비·최소 1초 대기를 지키며 접수 완료 또는 사용자 중지 시 끝납니다.
- 개인별 신청 제한, 서버 오류, 로그인, 최종 접수 결과 불명은 재시도 대상이 아닙니다.
- 반복 후 접수 성공·사용자 중지·개인별 제한 구분을 모의 테스트했습니다. 실제 서버 재시도는 미검증입니다.

## 1.8.1 — 조회 중 로그인 필요 상태 감지

- 로그인 화면·로그인 필요 안내를 감지하면 조회 중에도 정지하고 알립니다.
- 표시된 상단 로그인 메뉴가 3초 유지되면 정지합니다. 화면 초기 로딩 중 잠깐 보이는 메뉴는 기다리고, 로그인 여부가 불명확하면 단정하지 않습니다.
- 화면의 로그인·로그아웃 메뉴 변화만 기록하며 쿠키·계정 정보는 수집하지 않습니다. 서버 세션 만료 자체를 방지하는 기능은 아닙니다.
- 실제 사이트의 모든 헤더 형태와 로그아웃 원인은 미검증입니다.

## 1.8.0 — 한 번에 1명, 다음 사람은 직접 시작

- 여러 장 목표 선택과 자동 연속 예약을 제거했습니다. 코레일 조회 인원이 1명일 때만 시작합니다.
- 좌석 예약·예약대기 접수 후 정지하고 알립니다. 기존 접수를 유지하며 다음 1명은 내역 확인 동의 후 직접 시작합니다.
- 결과 불명·기존 접수로 일반 시작이 막힌 경우에도 명시적인 다음 사람 시작 버튼을 제공합니다. 실제 예약을 취소하지 않으며 현재 감시 조건을 사용합니다.
- 이전 여러 장 실행 상태는 업데이트 시 정지시킵니다. 기존 시도 기록은 다음 시작 확인 전까지 유지합니다.
- 1.7.4의 숨김 체크박스 대신 표시 라벨을 클릭하는 수정도 포함합니다.

## 1.7.4 — 예약대기 특실 허용 체크박스 수정

- 실제 로그에서 예약대기 신청창의 1픽셀 숨김 체크박스를 직접 클릭하다 정지하는 문제를 확인했습니다.
- 연결된 표시 라벨을 클릭하고 다음 단계에서 체크 상태를 확인합니다. 입력이 비활성화되었거나 라벨이 가려지면 제출하지 않습니다.
- 숨김 체크박스와 가려진 라벨 회귀 테스트를 추가했습니다. 실제 서버의 최종 대기 접수는 이 로그에서 확인되지 않았습니다.

## 1.7.3 — 패널 빈 공간 및 UI 정리

- 설정 영역이 남는 높이를 채우지 않고 내용에 맞게 줄어들도록 변경했습니다. 내용이 길 때 스크롤과 하단 실행 버튼은 유지합니다.
- 패널 바깥의 높이·여백·테두리를 명시하고 시간 입력칸의 너비 계산과 최소화 버튼의 축소 처리를 보완했습니다.
- 예약·대기 기록 초기화를 추가 설정으로 이동하고 진단 설명을 간소화했습니다. 기존 기능과 예매·조회 동작은 유지합니다.
- Windows 실화면에서의 재현은 아직 확인하지 못했습니다.

## 1.7.2 — 상세 진단 로그

기록만으로 모든 오류를 자동 수정하지는 않습니다. 로그를 통해 실패 위치를 구분하고 원인을 분석할 수 있도록 다음 정보를 추가했습니다.

- 실행 ID·문서 ID·연속 순번·발생 시각(eventAt)·저장 수신 시각(at). 자동 새로고침 시 실행 ID와 순번을 유지하고 수동 시작 시 새 실행 ID를 생성합니다.
- 좌석 등급·열차 번호·예매 단계와 단계 경과 시간·요청 응답 관측 여부. /web_r/ 타이밍 관측은 정확한 업무 요청 종류나 서버 내부 실패 원인을 보장하지 않습니다.
- 클릭 준비/위치 확인/전송/이벤트 확인 단계, 버튼명·활성/연결 상태·좌표·뷰포트·가림 여부, Chrome 입력 처리 세부 실패 단계와 원래 오류 사유.
- 중지 이유·예매 종료 이유·안내창 분류/문구/버튼·조회 HTTP 상태·페이지 종류·오류 이름·코드 줄/열 위치.
- 팝업 문구는 최대 1,200자만 기록합니다. URL·이메일·일반적인 전화번호와 예약번호 표기를 가립니다. 임의 형식의 개인정보까지 완벽하게 가려지는 것은 아니므로 외부 공유 전 로그를 확인하세요. 쿠키·요청/응답 본문·전체 페이지 HTML은 수집하지 않습니다.

HTTP 200은 예매 성공 판정이 아닙니다. 상세 로깅은 기존 재시도·정지 정책을 바꾸지 않습니다. 업데이트 이전 로그에 없던 정보는 복구되지 않습니다.

실패 시나리오 회귀 테스트와 파일 로그의 식별정보 마스킹을 검증했습니다.

## 1.7.1 — 로그 파일 삭제

로그 파일 목록에서 내려받기 옆 **삭제**를 누르고 파일명을 확인하면 해당 파일을 **완전삭제**합니다. 시스템 휴지통으로 보내지 않으며 복원할 수 없습니다. 취소하면 삭제하지 않습니다. Windows·Mac의 Chrome에서 같은 확장 프로그램 저장소 API를 사용합니다. 이미 내려받은 파일에는 영향을 주지 않습니다. 기록 중인 파일을 삭제하면 이후 기록은 새 파일에 저장됩니다. 현재 화면의 최근 진단 기록과 lastError는 별도의 탭 세션 데이터이므로 파일 삭제로 지워지지 않습니다. 두 OS의 실제 브라우저 검증은 하지 않았으며 파일 삭제·동시 쓰기·확인/취소·권한 제한 모의 테스트를 통과했습니다.

삭제와 기록을 직렬 처리하고, 확장 프로그램 로그 화면만 삭제 요청을 보낼 수 있도록 제한했습니다.

## 1.7.0 — 지속 로그와 gzip 압축

조회·좌석 발견·클릭·예매 요청 응답·중지·내부 오류를 확장 프로그램 전용 로컬 파일 저장소(OPFS)에 JSONL로 기록합니다. Chrome을 닫거나 PC를 재시작해도 보존되며, 새 버전을 로드한 이후 기록부터 저장합니다. 패널의 **진행 기록 → 로그 파일 · 내려받기**에서 파일별로 내려받을 수 있습니다. 일반 탐색기 폴더에 직접 저장하는 방식은 아닙니다.

- 크기 기준: UTF-8 기준 **10 MiB(10,485,760바이트)**. 다음 기록으로 한도를 넘기면 현재 파일을 gzip 압축하고 새 파일에 기록합니다.
- 날짜 기준: 한국 날짜가 바뀐 파일은 gzip 압축합니다. 기록할 때와 매시간 정리하며, Chrome이 꺼져 있으면 다음 실행 때 처리합니다.
- 압축 성공 후 원본 JSONL만 제거하고 압축본은 자동 삭제하지 않습니다. 압축 실패 시 원본을 유지합니다. 저장 공간은 계속 증가할 수 있습니다.
- 로그 기록은 순서대로 처리하고 자동 새로고침 전에 대기 중인 파일 쓰기를 기다립니다. 갑작스러운 강제 종료 직전 아직 전달되지 않은 기록은 누락될 수 있습니다.
- 쿠키와 요청 본문은 수집하지 않습니다. 열차 번호·오류 문구가 포함될 수 있습니다. 확장 프로그램 제거·브라우저 저장 데이터 삭제 시 로그도 삭제됩니다.
- 로컬 파일 저장소: https://developer.chrome.com/docs/capabilities/web-apis/file-system-access#accessing_the_origin_private_file_system / gzip: https://developer.mozilla.org/en-US/docs/Web/API/Compression_Streams_API

권한·검증: 매시간 정리에 `alarms` 권한을 사용합니다. 서비스 워커 재시작·동시 기록·압축 실패 복구를 모의 테스트했습니다.

## 1.6.1

- 마지막 내부 오류의 문구·단계·좌석 등급·직전 진행 기록을 같은 탭의 세션에 보존합니다. 재시작과 조회 기록 순환 후에도 진행 기록의 lastError에서 확인할 수 있습니다. 탭을 닫으면 삭제됩니다.
- 특실 선택과 오류 기록 보존에 대한 모의 회귀 테스트를 추가했습니다. 과거 예매 실패 원인은 미확정이며 실제 예매 성공을 검증한 변경은 아닙니다.

## 1.6.0 — 예약대기 신청 지원

- 자동 예매 모드에서 예약대기 선택 → 하단 신청 → 별도 대기신청 창까지 처리합니다. 현재 목록에서 즉시 예매 가능한 좌석을 우선합니다.
- 명확한 예약대기 접수 완료를 확인하면 정지하고 소리·PC·설정된 ntfy로 알립니다. HTTP 200이나 화면 이동만으로 성공 처리하지 않습니다.
- 예약대기는 확보 좌석 수에 포함하지 않으며, 웹 조회 인원으로 한 번 신청한 뒤 여러 장 목표와 관계없이 멈춥니다.
- 일반실 우선·특실 허용 설정을 신청창에 반영합니다. 선택 사항인 휴대폰 입력과 개인정보 동의는 자동으로 켜지 않습니다.
- 신청 오류·결과 불명 시 재전송하지 않으며, 기록 확인·초기화 전 재시작을 막아 중복 신청을 방지합니다.

검증: 신청 단계·좌석 우선·완료 판정·오류 및 문서 전환 중 중복 방지 모의 테스트. 실제 로그인 상태의 예약대기 접수 및 배정은 미검증입니다.

## 1.5.3 — 조회·안내 설정 간소화

- 안내 자동 확인과 실험적 탭 재조회 옵션을 제거했습니다.
- 조회는 항상 브라우저 일반 새로고침을 사용하며, 알려진 선택 열차 안내는 자동 확인합니다. 이전 옵션 저장값은 적용하지 않습니다.
- 사용하지 않는 탭 재조회 로직을 제거했습니다. 로그인·결제·알 수 없는 안내는 자동으로 진행하지 않습니다.

## 1.5.2 — 선택 열차 안내 자동 확인 통합

- 지연·우회·단체 위약금·좌석 자동배정의 개별 체크박스를 제거하고 선택 열차 안내 자동 확인으로 통합했습니다.
- 알려진 안내는 기본 자동 동의하며, 통합 스위치를 끄면 정지합니다. 이전 개별 설정값은 더 이상 적용하지 않습니다.
- 로그인·결제·알 수 없는 안내에 대한 중지 동작은 유지합니다.

## 1.5.1 — 체크박스 정렬 수정

- 일반 입력창의 높이·여백 스타일에서 체크박스를 분리했습니다.
- 추가 설정과 열차 선택 목록의 체크박스 크기를 고정하고 문구 첫 줄에 맞췄습니다.

## 1.5.0 — 같은 열차에서 여러 장 순차 예약

- 예약 목표 1~6장과 예약 진행 수량·첫 결제 기한 표시를 추가했습니다.
- 여러 장은 웹에서 1명으로 조회하며, 첫 예약 후 해당 열차·구간·시각으로 고정해 한 장씩 예약합니다. 연석은 요구하지 않습니다.
- 예약번호·날짜·열차·1명·배정 좌석을 확인한 뒤 집계합니다. 예약번호 또는 좌석 중복, 결과 불명은 추가 예약 없이 정지합니다.
- 확인된 예약 뒤 정상 조회 화면으로 이동하며, 검색 조건 복원 확인 후 남은 표를 찾습니다. 예약 요청 자체를 재전송하지 않습니다.
- 새로고침·중지 후 집계와 대상 열차를 유지합니다. 집계 초기화에는 별도 확인이 필요하며 실제 예약은 취소하지 않습니다.
- 각 예약 확인 시 알리고 목표 수량에서 정지합니다. 첫 결제 기한 2분 전 또는 기한 확인 실패 시에도 정지·알림합니다. 결제·취소 상태를 다른 탭과 동기화하지 않습니다.
- 예약 집계는 해당 탭의 세션 저장소에만 보관하며 진단 로그·ntfy 알림에는 예약번호를 보내지 않습니다.
- README를 설치·사용법 중심으로 정리하고 버전 이력을 이 문서로 분리했습니다.

검증: 모의 화면 기반 연속 예약·중복 집계·열차 고정·페이지 복원·결제 기한 회귀 테스트 통과. 로그인 후 실제 연속 예약 및 코레일 예약 상세 화면의 모든 형식은 아직 미검증이며, 읽지 못하는 화면에서는 정지합니다.

## 1.4.0 — 대상 열차 대시보드와 휴대폰 알림

- 대상 열차 번호·출발 시각·구간을 패널 상단에 고정 표시합니다.
- 예매 중에는 집중 열차와 재시도 횟수를, 최소화 상태에는 대상 번호 또는 시간대를 표시합니다.
- 설정 영역 스크롤과 하단 고정 버튼을 분리하고 작은 화면 배치를 보완했습니다.
- ntfy 연결 화면, 임의 주제 생성, 설정 저장 및 테스트 전송을 추가했습니다.
- 휴대폰에는 고정된 상태 문구만 전송합니다. 날짜·구간·예약번호·쿠키는 포함하지 않습니다.
- PC 알림 실패와 별개로 휴대폰 전송을 시도하고, 전송 실패를 패널에 표시합니다.
- 추가 권한: 로컬 설정용 `storage`, ntfy 활성화 시 요청하는 선택 권한 `https://ntfy.sh/*`.

검증: Chrome 화면 및 ntfy 서버 테스트 전송 확인, 자동 테스트 36개 실행 항목 통과. 아이폰 실제 수신은 별도 확인이 필요합니다.

## 1.3.6 — 화면 전환 후 새로고침 실패 수정

- 코레일 입력 화면에서 조회 목록으로 이동한 뒤에도 초기 문서 주소로 판단하던 문제를 수정했습니다.
- 같은 문서의 현재 경로와 실행 상태를 확인해 새로고침하며, 브라우저 입력 준비에도 같은 검증을 적용합니다.
- 새로고침 거절 사유를 패널과 진행 기록에 표시합니다.

검증: 실제 Chrome에서 입력 화면 → 조회 목록 → 자동 새로고침 및 후속 HTTP 200 응답을 확인했습니다.

## 1.3.5 — 잔여석 없음 재시도와 알림 개선

- 예매 후 명확한 잔여석 없음 안내가 나오면 닫고, 선택한 열차·좌석을 유지해 예매 버튼만 재시도합니다.
- 안내창 종료와 화면 준비를 확인하며 새로고침·열차 재선택·중복 요청을 하지 않습니다.
- 일반 실패, HTTP 오류, 결과 불명, 로그인·접근 제한은 자동 재시도와 구분해 정지합니다.
- 반복 비프음을 부드럽게 잦아드는 차임으로 변경했습니다.
- PC 알림 테스트와 권한·생성 실패 표시를 추가했습니다.

검증: 반복 잔여석 응답, 동일 열차 유지, 안내창·로딩 대기, 중지 및 알림 실패 회귀 테스트. 실제 매진 경쟁 상황의 좌석 확보는 미검증입니다.

## 1.3.4 — 브라우저 입력 방식 변경

- DOM `.click()` 대신 Chrome `debugger` API의 마우스 입력을 사용합니다.
- 코레일 조회 문서, 버튼 좌표, 가림 여부와 실행 상태를 확인한 뒤 클릭합니다.
- 연결 실패 시 합성 클릭으로 대체하지 않고 정지합니다. 버튼을 가리는 패널은 자동 최소화합니다.
- `debugger` 권한이 추가됐습니다. 개발자 도구와 연결이 충돌할 수 있습니다.

검증: 동일 조건에서 기존 자동 클릭의 HTTP 500과 새 입력 방식의 HTTP 200·로그인 화면 이동을 비교했습니다. 서버 내부 거부 원인이나 로그인 후 예약 성공까지 입증한 것은 아닙니다.

## 1.3.3 — 쉬어가기

- 정해진 조회 횟수마다 쉬는 설정과 대기 상태 표시를 추가했습니다.
- 오류로 늘어난 대기와 휴식이 겹치면 더 긴 시간을 적용합니다.

## 1.3.2 — 감시 대상 표시

- 감시 대상 열차에 점선 표시를 적용하고, 패널에 현재 목록의 대상 개수를 표시합니다.
- 목록 갱신 시 표시를 다시 적용하며 정지하면 해제합니다.

## 1.3.1 — 수서 열차와 알림 보완

- 특정 열차 모드에서 선택한 수서·인접역 열차가 검색 구간 차이로 제외되는 문제를 보완했습니다.
- 이전 실행의 강조 표시를 정리하고 예상치 못한 감시 중지를 알리도록 보완했습니다.

## 1.3.0 — 조회 복구와 예매 안내창 처리

- 빈 조회 결과를 실제 운행 없음과 조회 실패 모두 가능한 상태로 처리하고 복구를 시도합니다.
- 좌석 선택, 안내 확인, 요청 전송, 결과 확인 단계를 구분했습니다.
- 정차역·편성·할인 안내와 조건별 동의창을 구분하며, 구입 조건 변경은 설정에서 켠 경우에만 확인합니다.
- 좌석 칸 상태 판정, 입석+좌석 예매 버튼, 예약 상세 화면 인식을 보완했습니다.
- HTTP 응답 기록과 오류에 따른 대기 조정을 추가하고, 탭 재조회를 실험적 선택 항목으로 제공했습니다.

## 1.1.14 — 브라우저 새로고침

- 서비스 워커의 `chrome.tabs.reload`로 해당 조회 탭에 일반 새로고침을 요청하도록 변경했습니다.
- 새로고침 중 중복 요청과 실패·시간 초과 처리를 추가했습니다.

## 1.1.13 — 빈 결과 복구와 시간대 더보기

- 빈 결과·조회 오류 복구와 더보기 응답 대기를 보완했습니다.
- 종료 시각을 포함하는 목록까지 검사하고, 개수뿐 아니라 열차 내용 변화로 더보기 결과를 확인합니다.

## 1.1.12 — 예매 요청 오류 감지

- 예매 클릭 이후 요청의 HTTP 오류를 감지하고, 결과를 모르는 상태에서 중복 요청하지 않도록 보완했습니다.
- 진단 기록에는 URL·쿼리 토큰·쿠키·요청 및 응답 본문을 남기지 않습니다.

## 1.1.11 — 예매 안정화와 진단 기록

- 좌석 선택·안내 확인 뒤 로딩 종료와 버튼 활성화가 유지되는지 확인하고 예매를 누릅니다.
- 클릭 이벤트와 요청 진행 기록을 추가했습니다. 당시 자동 예매 실패 원인은 미해결 상태였습니다.

## 1.1.9 — 감시 조건 분리와 패널 최소화

- 시간대와 열차 번호를 동시에 적용하던 조건을 서로 독립적인 두 모드로 분리했습니다.
- 패널 최소화, 설정 접힘 상태 유지 및 추가 설정 구성을 보완했습니다.
