# 릴리스 노트

[설치·사용법으로 돌아가기](README.md)

최신 버전부터 정리한 변경 이력입니다. 각 항목은 해당 버전 출시 당시의 동작을 설명하며, 현재 사용법은 README를 참고하세요.

## 1.5.0 — 같은 열차에서 여러 장 순차 예약

- 예약 목표 1~6장과 예약 진행 수량·첫 결제 기한 표시를 추가했습니다.
- 여러 장은 웹에서 1명으로 조회하며, 첫 예약 후 해당 열차·구간·시각으로 고정해 한 장씩 예약합니다. 연석은 요구하지 않습니다.
- 예약번호·날짜·열차·1명·배정 좌석을 확인한 뒤 집계합니다. 예약번호 또는 좌석 중복, 결과 불명은 추가 예약 없이 정지합니다.
- 확인된 예약 뒤 정상 조회 화면으로 이동하며, 검색 조건 복원 확인 후 남은 표를 찾습니다. 예약 요청 자체를 재전송하지 않습니다.
- 새로고침·중지 후 집계와 대상 열차를 유지합니다. 집계 초기화에는 별도 확인이 필요하며 실제 예약은 취소하지 않습니다.
- 각 예약 확인 시 알리고 목표 수량에서 정지합니다. 첫 결제 기한 2분 전 또는 기한 확인 실패 시에도 정지·알림합니다. 결제·취소 상태를 다른 탭과 동기화하지 않습니다.
- 예약 집계는 해당 탭의 세션 저장소에만 보관하며 진단 로그·ntfy 알림에는 예약번호를 보내지 않습니다.
- README를 설치·사용법 중심으로 정리하고 버전 이력을 이 문서로 분리했습니다.

검증: 모의 화면 기반 연속 예약·중복 집계·열차 고정·페이지 복원·결제 기한 회귀 테스트 통과. 로그인 후 실제 연속 예약 및 코레일 예약 상세 화면의 모든 형식은 아직 미검증이며, 읽지 못하는 화면에서는 정지합니다.

## 1.4.0 — 대상 열차 대시보드와 휴대폰 알림

- 대상 열차 번호·출발 시각·구간을 패널 상단에 고정 표시합니다.
- 예매 중에는 집중 열차와 재시도 횟수를, 최소화 상태에는 대상 번호 또는 시간대를 표시합니다.
- 설정 영역 스크롤과 하단 고정 버튼을 분리하고 작은 화면 배치를 보완했습니다.
- ntfy 연결 화면, 임의 주제 생성, 설정 저장 및 테스트 전송을 추가했습니다.
- 휴대폰에는 고정된 상태 문구만 전송합니다. 날짜·구간·예약번호·쿠키는 포함하지 않습니다.
- PC 알림 실패와 별개로 휴대폰 전송을 시도하고, 전송 실패를 패널에 표시합니다.
- 추가 권한: 로컬 설정용 `storage`, ntfy 활성화 시 요청하는 선택 권한 `https://ntfy.sh/*`.

검증: Chrome 화면 및 ntfy 서버 테스트 전송 확인, 자동 테스트 36개 실행 항목 통과. 아이폰 실제 수신은 별도 확인이 필요합니다.

## 1.3.6 — 화면 전환 후 새로고침 실패 수정

- 코레일 입력 화면에서 조회 목록으로 이동한 뒤에도 초기 문서 주소로 판단하던 문제를 수정했습니다.
- 같은 문서의 현재 경로와 실행 상태를 확인해 새로고침하며, 브라우저 입력 준비에도 같은 검증을 적용합니다.
- 새로고침 거절 사유를 패널과 진행 기록에 표시합니다.

검증: 실제 Chrome에서 입력 화면 → 조회 목록 → 자동 새로고침 및 후속 HTTP 200 응답을 확인했습니다.

## 1.3.5 — 잔여석 없음 재시도와 알림 개선

- 예매 후 명확한 잔여석 없음 안내가 나오면 닫고, 선택한 열차·좌석을 유지해 예매 버튼만 재시도합니다.
- 안내창 종료와 화면 준비를 확인하며 새로고침·열차 재선택·중복 요청을 하지 않습니다.
- 일반 실패, HTTP 오류, 결과 불명, 로그인·접근 제한은 자동 재시도와 구분해 정지합니다.
- 반복 비프음을 부드럽게 잦아드는 차임으로 변경했습니다.
- PC 알림 테스트와 권한·생성 실패 표시를 추가했습니다.

검증: 반복 잔여석 응답, 동일 열차 유지, 안내창·로딩 대기, 중지 및 알림 실패 회귀 테스트. 실제 매진 경쟁 상황의 좌석 확보는 미검증입니다.

## 1.3.4 — 브라우저 입력 방식 변경

- DOM `.click()` 대신 Chrome `debugger` API의 마우스 입력을 사용합니다.
- 코레일 조회 문서, 버튼 좌표, 가림 여부와 실행 상태를 확인한 뒤 클릭합니다.
- 연결 실패 시 합성 클릭으로 대체하지 않고 정지합니다. 버튼을 가리는 패널은 자동 최소화합니다.
- `debugger` 권한이 추가됐습니다. 개발자 도구와 연결이 충돌할 수 있습니다.

검증: 동일 조건에서 기존 자동 클릭의 HTTP 500과 새 입력 방식의 HTTP 200·로그인 화면 이동을 비교했습니다. 서버 내부 거부 원인이나 로그인 후 예약 성공까지 입증한 것은 아닙니다.

## 1.3.3 — 쉬어가기

- 정해진 조회 횟수마다 쉬는 설정과 대기 상태 표시를 추가했습니다.
- 오류로 늘어난 대기와 휴식이 겹치면 더 긴 시간을 적용합니다.

## 1.3.2 — 감시 대상 표시

- 감시 대상 열차에 점선 표시를 적용하고, 패널에 현재 목록의 대상 개수를 표시합니다.
- 목록 갱신 시 표시를 다시 적용하며 정지하면 해제합니다.

## 1.3.1 — 수서 열차와 알림 보완

- 특정 열차 모드에서 선택한 수서·인접역 열차가 검색 구간 차이로 제외되는 문제를 보완했습니다.
- 이전 실행의 강조 표시를 정리하고 예상치 못한 감시 중지를 알리도록 보완했습니다.

## 1.3.0 — 조회 복구와 예매 안내창 처리

- 빈 조회 결과를 실제 운행 없음과 조회 실패 모두 가능한 상태로 처리하고 복구를 시도합니다.
- 좌석 선택, 안내 확인, 요청 전송, 결과 확인 단계를 구분했습니다.
- 정차역·편성·할인 안내와 조건별 동의창을 구분하며, 구입 조건 변경은 설정에서 켠 경우에만 확인합니다.
- 좌석 칸 상태 판정, 입석+좌석 예매 버튼, 예약 상세 화면 인식을 보완했습니다.
- HTTP 응답 기록과 오류에 따른 대기 조정을 추가하고, 탭 재조회를 실험적 선택 항목으로 제공했습니다.

## 1.1.14 — 브라우저 새로고침

- 서비스 워커의 `chrome.tabs.reload`로 해당 조회 탭에 일반 새로고침을 요청하도록 변경했습니다.
- 새로고침 중 중복 요청과 실패·시간 초과 처리를 추가했습니다.

## 1.1.13 — 빈 결과 복구와 시간대 더보기

- 빈 결과·조회 오류 복구와 더보기 응답 대기를 보완했습니다.
- 종료 시각을 포함하는 목록까지 검사하고, 개수뿐 아니라 열차 내용 변화로 더보기 결과를 확인합니다.

## 1.1.12 — 예매 요청 오류 감지

- 예매 클릭 이후 요청의 HTTP 오류를 감지하고, 결과를 모르는 상태에서 중복 요청하지 않도록 보완했습니다.
- 진단 기록에는 URL·쿼리 토큰·쿠키·요청 및 응답 본문을 남기지 않습니다.

## 1.1.11 — 예매 안정화와 진단 기록

- 좌석 선택·안내 확인 뒤 로딩 종료와 버튼 활성화가 유지되는지 확인하고 예매를 누릅니다.
- 클릭 이벤트와 요청 진행 기록을 추가했습니다. 당시 자동 예매 실패 원인은 미해결 상태였습니다.

## 1.1.9 — 감시 조건 분리와 패널 최소화

- 시간대와 열차 번호를 동시에 적용하던 조건을 서로 독립적인 두 모드로 분리했습니다.
- 패널 최소화, 설정 접힘 상태 유지 및 추가 설정 구성을 보완했습니다.
